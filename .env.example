# ========================================
# Dólar Gaucho - Environment Variables
# ========================================

# ========================================
# AUTHENTICATION (REQUIRED FOR PRODUCTION)
# ========================================

# JWT Secret Key (REQUIRED)
# -----------------
# Secret key for signing JWT tokens
# MUST be at least 32 characters long
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-minimum-32-characters

# JWT Expiration (Optional)
# -----------------
# How long JWT tokens are valid
# Examples: 7d (7 days), 24h (24 hours), 30m (30 minutes)
JWT_EXPIRATION=7d

# ========================================
# DATABASE (REQUIRED FOR PRODUCTION)
# ========================================

# PostgreSQL Connection (Vercel Postgres)
# -----------------
# If using Vercel Postgres, these are automatically set by Vercel
# For local development, get the connection string from Vercel dashboard
POSTGRES_URL=your-postgres-connection-string-here

# Alternative: Manual PostgreSQL configuration
# -----------------
# If not using Vercel Postgres, configure manually:
# DATABASE_URL=postgresql://user:password@host:5432/database

# ========================================
# NEXT.JS CONFIGURATION
# ========================================

# Node Environment
# -----------------
NODE_ENV=development

# App URL (Optional)
# -----------------
# Used for redirects and absolute URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000

# API Configuration (Optional)
# -----------------
# URL base para el API interno
NEXT_PUBLIC_API_URL=/api

# ========================================
# EMAIL SERVICE (OPTIONAL BUT RECOMMENDED)
# ========================================

# Resend API Key (REQUIRED for email functionality)
# -----------------
# Used for:
# - Welcome emails cuando el usuario se registra
# - Password reset emails para recuperación de contraseña
# - Alert notifications cuando se disparan alertas de precio
#
# IMPORTANTE: Sin esta key configurada:
# - Los usuarios NO recibirán emails de bienvenida al registrarse
# - NO funcionará la recuperación de contraseña
# - NO se enviarán notificaciones de alertas
#
# Cómo obtener tu API key:
# 1. Crear cuenta en https://resend.com
# 2. Ir a https://resend.com/api-keys
# 3. Crear una nueva API key
# 4. Copiar y pegar aquí (formato: re_xxxxxxxxxxxxxxxxxxxxx)
# 5. Configurar en Vercel Dashboard > Settings > Environment Variables
#
# Free tier: 3,000 emails/mes (suficiente para empezar)
RESEND_API_KEY=your-resend-api-key-here

# Email From Address (Optional)
# -----------------
# The "from" address for all emails
# Must be a verified domain in Resend
EMAIL_FROM=Dólar Gaucho <noreply@dolargaucho.com>

# Email Reply-To Address (Optional)
# -----------------
# Where users can reply to emails
EMAIL_REPLY_TO=soporte@dolargaucho.com

# ========================================
# CRON JOBS (OPTIONAL BUT RECOMMENDED FOR ALERTS)
# ========================================

# Cron Secret (Optional - for Vercel Cron authentication)
# -----------------
# Secret token to authenticate Vercel Cron Jobs
# Used to protect cron endpoints from unauthorized access
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Configure in Vercel Dashboard > Settings > Environment Variables
CRON_SECRET=your-cron-secret-change-this-in-production

# ========================================
# EXTERNAL APIS (OPTIONAL)
# ========================================

# FRED API (Optional - Only for US inflation data)
# -----------------
# API Key from Federal Reserve Economic Data
# Get it at: https://fred.stlouisfed.org/docs/api/api_key.html
NEXT_PUBLIC_FRED_API_KEY=your-fred-api-key-here

# ========================================
# IMPORTANT NOTES:
# ========================================
#
# 1. Copy this file to '.env.local' for development:
#    cp .env.example .env.local
#
# 2. Public APIs that DON'T require configuration:
#    - DolarAPI (https://dolarapi.com/v1)
#    - ArgentinaData (https://api.argentinadatos.com/v1)
#
# 3. REQUIRED for production:
#    - JWT_SECRET (generate a secure random string)
#    - POSTGRES_URL (database connection string)
#
# 3.5 RECOMMENDED for production (optional but highly recommended):
#    - RESEND_API_KEY (for transactional emails)
#    - EMAIL_FROM (verified sender address)
#    - EMAIL_REPLY_TO (support email)
#
# 4. NEVER commit the .env.local file
#    (already in .gitignore)
#
# 5. For Vercel deployment:
#    - Set environment variables in Vercel dashboard
#    - POSTGRES_URL is auto-set when you create Vercel Postgres
#    - You only need to manually set JWT_SECRET
#
